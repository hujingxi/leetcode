class Solution(object):
    def arrayPairSum(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        min_sum = 0
        nums.sort()
        for i in range(0,len(nums),2):
            min_sum += nums[i]
        return min_sum
